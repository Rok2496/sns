import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import Header from '@/components/Header'
import Footer from '@/components/Footer'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Star Network Solutions - Empowering Your Digital Transformation',
  description: 'SNS provides complete IT infrastructure solutions, security, networking, and professional services to transform your business.',
  keywords: 'IT solutions, network security, data center, server virtualization, Bangladesh IT company',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Header />
        <main>{children}</main>
        <Footer />
      </body>
    </html>
  )
}